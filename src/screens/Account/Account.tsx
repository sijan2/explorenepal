import { Text } from "react-native"
import React from "react"
import { SafeAreaView } from "react-native-safe-area-context"

const Account = () => {
  return (
    <SafeAreaView className="flex-1 bg-slate-300 px-4">
      <Text>Account</Text>
    </SafeAreaView>
  )
}

export default Account
